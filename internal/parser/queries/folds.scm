;; Block statements
(block) @fold

;; Configuration blocks
(mcp_config_block) @fold
(model_config_block) @fold
(agent_config_block) @fold

;; Object and array literals
(object_literal) @fold
(array_literal) @fold
(map_literal) @fold

;; Multi-line strings
(string_literal) @fold

;; Match expressions
(match_expression) @fold

;; Comments
(comment) @fold